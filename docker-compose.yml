version: "3.9"

services:
  nginx:
    image: nginx:alpine
    container_name: netshield-nginx
    ports:
      - "80:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_started
    restart: unless-stopped
    networks:
      - netshield-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: netshield-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: >
      sh -c "python -m scripts.create_admin &&
             alembic stamp head &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000"
    environment:
      - PYTHONPATH=/app
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-netshield}:${POSTGRES_PASSWORD:-netshield_secret}@db:5432/${POSTGRES_DB:-netshield_db}
      - MONGO_URL=mongodb://${MONGO_USER:-netshield}:${MONGO_PASSWORD:-netshield_mongo}@mongodb:27017/${MONGO_DB:-netshield_traffic}?authSource=admin
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/5
      - CELERY_RESULT_BACKEND=redis://redis:6379/5
      - ADMIN_EMAIL=admin@netshield.io
      - ADMIN_PASSWORD=Admin@123
    volumes:
      - ./backend:/app
      - ./logging:/app/logging
      - ./scripts:/app/scripts
      - ./reports:/reports
    depends_on:
      db:
        condition: service_healthy
      mongodb:
        condition: service_started
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/api/v1/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    networks:
      - netshield-network

  celery_worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: netshield-celery-worker
    command: celery -A app.tasks.celery_app worker --loglevel=info --concurrency=4
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-netshield}:${POSTGRES_PASSWORD:-netshield_secret}@db:5432/${POSTGRES_DB:-netshield_db}
      - MONGO_URL=mongodb://${MONGO_USER:-netshield}:${MONGO_PASSWORD:-netshield_mongo}@mongodb:27017/${MONGO_DB:-netshield_traffic}?authSource=admin
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/5
      - CELERY_RESULT_BACKEND=redis://redis:6379/5
    volumes:
      - ./backend:/app
      - ./logging:/app/logging
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - netshield-network

  celery_beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: netshield-celery-beat
    command: celery -A app.tasks.celery_app beat --loglevel=info
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-netshield}:${POSTGRES_PASSWORD:-netshield_secret}@db:5432/${POSTGRES_DB:-netshield_db}
      - MONGO_URL=mongodb://${MONGO_USER:-netshield}:${MONGO_PASSWORD:-netshield_mongo}@mongodb:27017/${MONGO_DB:-netshield_traffic}?authSource=admin
      - REDIS_URL=redis://redis:6379/0
      - CELERY_BROKER_URL=redis://redis:6379/5
      - CELERY_RESULT_BACKEND=redis://redis:6379/5
    volumes:
      - ./backend:/app
    depends_on:
      backend:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - netshield-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://localhost/api/v1
        - NEXT_PUBLIC_WS_URL=ws://localhost/ws/alerts
    container_name: netshield-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost/api/v1
      - NEXT_PUBLIC_WS_URL=ws://localhost/ws/alerts
    volumes:
      - ./frontend/src:/app/src
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - netshield-network

  db:
    image: postgres:16-alpine
    container_name: netshield-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-netshield}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-netshield_secret}
      POSTGRES_DB: ${POSTGRES_DB:-netshield_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-netshield}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - netshield-network

  mongodb:
    image: mongo:7
    container_name: netshield-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-netshield}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-netshield_mongo}
      MONGO_INITDB_DATABASE: ${MONGO_DB:-netshield_traffic}
    volumes:
      - mongo_data:/data/db
      - ./docker/mongo/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js
    restart: unless-stopped
    networks:
      - netshield-network

  redis:
    image: redis:7-alpine
    container_name: netshield-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - netshield-network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: netshield-pgadmin
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@netshield.io}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - db
    restart: unless-stopped
    networks:
      - netshield-network

  mongo-express:
    image: mongo-express:latest
    container_name: netshield-mongo-express
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: ${MONGO_USER:-netshield}
      ME_CONFIG_MONGODB_ADMINPASSWORD: ${MONGO_PASSWORD:-netshield_mongo}
      ME_CONFIG_MONGODB_SERVER: mongodb
      ME_CONFIG_BASICAUTH_USERNAME: ${ME_USER:-admin}
      ME_CONFIG_BASICAUTH_PASSWORD: ${ME_PASSWORD:-admin}
    depends_on:
      - mongodb
    restart: unless-stopped
    networks:
      - netshield-network

volumes:
  postgres_data:
  mongo_data:
  redis_data:
  pgadmin_data:

networks:
  netshield-network:
    driver: bridge
